{
  "name": "greenmd-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Test-only tooling for GreenMD. Nothing here ships with the application: the app itself has no JavaScript dependencies at all.",
  "scripts": {
    "test": "node lint-sources.mjs && node ui-test.mjs",
    "lint": "node lint-sources.mjs",
    "ui": "node ui-test.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
